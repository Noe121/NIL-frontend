# Example config for automated test environment switching
# Copy to test.env and fill in values as needed

# Local/Docker defaults
REACT_APP_API_URL=http://localhost:8000/
AUTH_SERVICE_URL=http://localhost:8001/login

# Cloud override (auto-populated from Terraform outputs if present)
# These will be set by the test script if outputs.json exists
# API_URL and AUTH_SERVICE_URL will be replaced automatically
# Example outputs.json:
# {
#   "api_url": { "value": "https://api.cloud.example.com/" },
#   "auth_service_url": { "value": "https://auth.cloud.example.com/login" }
# }

# Usage:
# 1. Copy this file to test.env and set local values if needed
# 2. Run tests as usual (npm run test)
# 3. If NILbx-env/outputs.json exists, cloud endpoints will be used automatically
